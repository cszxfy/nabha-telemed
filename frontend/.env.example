# Copy this file to .env for local development.
# Use true for the self-contained demo API; set false when your real backend routes are ready.
VITE_DEMO_MODE=true
VITE_API_BASE_URL=/api/v1
VITE_BACKEND_URL=http://localhost:5000
